# Page Map

สรุปหน้าทั้งหมดของโปรเจกต์ `business-ops-tracker` แบบอ่านเร็ว แยกเป็น “ปัจจุบัน” และ “อนาคต” เพื่อใช้เป็นแผนพัฒนาต่อ

## `/`

**ปัจจุบัน**
- หน้าเริ่มต้นของแอป
- พาไปหน้าหลักที่ใช้งานจริง คือ `/expenses`

**อนาคต**
- ถ้าจะมี landing page จริง ควรใส่สถานะล็อกอิน, ปุ่มเข้าแอป, และสรุปภาพรวมสั้น ๆ

## `/expenses`

**ปัจจุบัน**
- หน้า dashboard หลักของรายจ่าย
- แสดงรายการรายจ่ายจาก Firestore แบบ realtime
- มีสรุปรายเดือน/ตัวกรอง/ค้นหา/ลบ/ส่งออก CSV
- ใช้เป็นศูนย์กลางงานประจำวันของระบบ

**อนาคต**
- เพิ่มมุมมอง analytics มากขึ้น เช่น trend, top store, top category
- เพิ่ม quick actions เช่น สร้างรายจ่ายใหม่จาก template

## `/expenses/new`

**ปัจจุบัน**
- สร้างรายจ่ายใหม่จากรูปใบเสร็จ
- รองรับอัปโหลดรูป, OCR, Gemini extraction, แก้ข้อมูล, แล้วค่อยบันทึกลง Firestore
- เป็น flow หลักสำหรับสร้างข้อมูลใหม่

**อนาคต**
- ลดขั้นตอนการกรอกซ้ำ
- เพิ่ม auto-detect วันที่/สกุลเงิน/ร้านค้าให้แม่นขึ้น
- ทำ mobile-first flow ให้สั้นลง

## `/expenses/[id]`

**ปัจจุบัน**
- หน้ารายละเอียดและแก้ไขรายจ่ายเดิม
- โหลดข้อมูลจาก Firestore
- แก้รูป, แก้รายการสินค้า, รัน OCR/Gemini ใหม่, และบันทึกทับข้อมูลเดิมได้

**อนาคต**
- เพิ่ม history ของการแก้ไข
- เพิ่ม diff เทียบก่อน-หลังแก้
- ทำปุ่มรีเฟรชเรท/รีคำนวณให้เด่นขึ้น

## `/export`

**ปัจจุบัน**
- หน้าสำหรับ export/import ข้อมูลของระบบ local legacy
- ใช้สำรองข้อมูลหรือย้ายข้อมูล

**อนาคต**
- รวม export ของ cloud data กับ local data ให้ชัดเจนขึ้น
- เพิ่ม export เป็นหลายรูปแบบ เช่น JSON, CSV, ZIP

## `/mobile`

**ปัจจุบัน**
- หน้าสรุปแบบมือถือ
- จัด layout ให้เหมาะกับจอเล็กและการใช้งานเร็ว
- เหมาะเป็นหน้าใช้งานลดความซับซ้อน

**อนาคต**
- ทำให้เป็น mobile home หลัก
- ใส่ bottom nav, quick add, recent receipts, และ pending tasks

## `/offline`

**ปัจจุบัน**
- หน้าแจ้งสถานะออฟไลน์
- ใช้เป็น fallback เมื่อเข้าแอปไม่ได้

**อนาคต**
- เชื่อมกับ PWA/offline cache ให้มากขึ้น
- แสดงสิ่งที่ยังทำได้แม้ไม่มีเน็ต

## `/products`

**ปัจจุบัน**
- หน้ารวมสินค้า/บริการจากทุกใบเสร็จในธุรกิจเดียวกัน
- รวมชื่อสินค้าที่เหมือนกันข้ามร้าน
- แสดงจำนวนรวม, มูลค่ารวม, ราคาเฉลี่ย, และสลับสกุลเงินได้

**อนาคต**
- เพิ่ม ranking สินค้าขายดี
- เพิ่ม filter ตามร้าน, หมวด, ช่วงเวลา
- เพิ่มการ drill-down ไปดูประวัติแต่ละสินค้า

## `/receipt-chat`

**ปัจจุบัน**
- หน้าถ่ายรูปใบเสร็จแบบ chat-style
- ใช้ Vision + Gemini เหมือน flow หลัก แต่ UX เป็นแนวสนทนา
- มีการแก้ OCR, แก้รายการ, และบันทึกผล

**อนาคต**
- ทำให้เป็น flow มือถือหลัก
- ลดการเลื่อนซ้ายขวาและล็อกส่วนสำคัญไม่ให้กระโดด
- เพิ่ม loading/progress ที่สื่อสถานะชัดขึ้น

## `/receipts/new`

**ปัจจุบัน**
- หน้า legacy สำหรับสร้าง receipt local
- ใช้ OCR, preprocess รูป, และช่วยเริ่มต้นข้อมูลในฐาน local

**อนาคต**
- ถ้า legacy flow ไม่จำเป็น อาจรวมเข้าสู่ cloud flow
- ถ้ายังเก็บไว้ ควรใส่คำอธิบายว่าเป็น local mode

## `/receipts/review`

**ปัจจุบัน**
- หน้า review OCR และ Gemini extraction ของ flow local
- ใช้แก้ข้อมูลก่อนบันทึกลงฐาน local

**อนาคต**
- ใช้เป็นหน้าทดสอบ extraction pipeline
- เพิ่ม validation และ highlight จุดที่ Gemini ไม่มั่นใจ

## `/receipts/history`

**ปัจจุบัน**
- หน้าแสดงประวัติใบเสร็จ local
- ใช้ค้นหา ดูรายการ และลบข้อมูลใน legacy DB

**อนาคต**
- เพิ่ม filters ให้ละเอียดขึ้น
- เพิ่มเปรียบเทียบประวัติการแก้ไข/รูปเดิม

## `/settings/businesses`

**ปัจจุบัน**
- จัดการธุรกิจ
- สร้าง/แก้ไข/เลือกธุรกิจที่ active
- เป็นหน้าสำคัญของโครงสร้าง Firestore ปัจจุบัน

**อนาคต**
- เพิ่มจัดการหลาย workspace อย่างชัดเจน
- เพิ่มรายละเอียดสิทธิ์ผู้ใช้/owner

## `/settings/categories`

**ปัจจุบัน**
- จัดการหมวดหมู่และกฎการจัดประเภท
- มีการตั้งลิมิตการใช้งาน Vision/Gemini ฝั่ง local

**อนาคต**
- ทำ rule editor ที่เข้าใจง่ายขึ้น
- เพิ่ม preview ว่า rule ไหนจะจับอะไร

## API routes

### `/api/auth/[...nextauth]`

**ปัจจุบัน**
- Google OAuth login ผ่าน NextAuth

**อนาคต**
- เพิ่ม custom session claims ถ้าจำเป็น

### `/api/vision/ocr`

**ปัจจุบัน**
- รับรูปแล้วเรียก Google Vision OCR
- ใช้เป็นขั้นตอนแรกของการอ่านใบเสร็จ

**อนาคต**
- เพิ่ม retry และ better fallback เมื่อ OCR fail

### `/api/gemini/extract-receipt`

**ปัจจุบัน**
- รับ OCR text แล้วให้ Gemini แปลงเป็น structured JSON
- ใช้สำหรับแปลงเป็นร้านค้า, วันที่, รายการ, ยอดเงิน

**อนาคต**
- เพิ่ม prompt/version management
- เพิ่มโมเดล fallback หลายตัวแบบควบคุมได้

### `/api/exchange-rate`

**ปัจจุบัน**
- รับ `from`, `to`, `date`
- ดึงอัตราแลกเปลี่ยนย้อนหลังจาก Frankfurter
- ตอนนี้รองรับแค่ `THB` กับ `JPY`

**อนาคต**
- ถ้าจะรองรับหลายสกุล ควรทำแคชและ guard ให้ชัด

### `/api/google/*`

**ปัจจุบัน**
- ชุด route สำหรับอัปโหลดรูป, บันทึกลง Google Drive/Sheets, และดึงรูปจาก Drive

**อนาคต**
- เพิ่ม health check และแยกสถานะ sync ให้ชัดขึ้น

## สรุปภาพรวม

**ปัจจุบัน**
- แอปนี้เป็น receipt management system ที่มี 2 โลกอยู่พร้อมกัน:
  - cloud-first บน Firestore + Google Drive
  - legacy local บน IndexedDB
- เส้นหลักที่ใช้งานจริงตอนนี้คือ `/expenses`, `/expenses/new`, `/expenses/[id]`, `/products`, `/receipt-chat`

**อนาคต**
- ควรค่อย ๆ รวมประสบการณ์ให้เหลือ flow หลักเดียว
- ทำ mobile-first ให้ชัดขึ้น
- ลดความซ้ำระหว่าง cloud และ local
- ทำ exchange rate, Gemini, และ OCR ให้เสถียรและ predictable มากขึ้น
